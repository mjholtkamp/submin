#!/usr/bin/python
import os
import sys

submin_root = os.path.dirname(os.path.dirname(__file__))
path = os.path.realpath(submin_root)
sys.path.insert(0, path)

import submin
from submin.subminadmin import SubminAdmin

sa = SubminAdmin(sys.argv)
success = sa.run()
if not success:
	sys.exit(1)
